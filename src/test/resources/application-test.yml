spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  mail:
    host: localhost
    port: 25
    username: test@example.com
    password: testpassword

slack:
  webhook:
    url: http://localhost/mock-slack

groq:
  api:
    key: mock-key
    url: http://localhost/mock-groq
  model: mock-model
